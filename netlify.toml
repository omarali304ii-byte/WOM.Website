[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22.13.0"
  NPM_FLAGS = "--include=dev"

[dev]
  command = "npm run dev"
  targetPort = 3000
  framework = "#auto"

